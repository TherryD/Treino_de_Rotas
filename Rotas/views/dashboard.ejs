<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Painel Principal</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
  <div class="painel-principal">
    <% if (usuario) { %>
      <h1>Bem-vindo(a), <%= usuario.nome %>!</h1>
      <p>Gerencie suas anotações abaixo.</p>
      <div class="actions">
        <a href="/criar" class="btn btn-anotacao">Criar Nova Anotação</a>
        <a href="/<%= usuario.id %>/lixeira" class="btn btn-secondary">Lixeira</a>
        <a href="/<%= usuario.id %>/perfil" class="btn btn-secondary">Meu Perfil</a>
        <a href="/lougout" class="btn btn-secondary">Sair</a>
      </div>
      <hr style="margin: 30px 0;">

      <h2>Suas Anotações</h2>
      <% if (anotacoes.length > 0) { %>
        <ul class="lista-anotacoes">
          <% anotacoes.forEach(function(anotacao) { %>
            <a href="/<%= usuario.id %>/<%= anotacao.id %>" class="link-anotacao">
                <li class="item-anotacao">
                    <span class="nome-anotacao"><%= anotacao.nome %></span>
                </li>
            </a> 
        <% }); %>
        </ul>
      <% } else { %>
        <p>Você ainda não tem nenhuma anotação. Que tal criar a primeira?</p>
      <% } %>
    <% } else { %>
      
      <h1>Erro</h1>
      <p>Você precisa estar logado para ver esta página.</p>
      <a href="/login">Fazer Login</a>
    <% } %>
  </div>
</body>
</html>