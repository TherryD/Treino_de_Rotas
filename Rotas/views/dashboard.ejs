<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Painel Principal</title>
  <link rel='stylesheet' href='/stylesheets/style.css'/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"/>
</head>
<body>
  <div class="painel-principal">
    <% if (usuario) { %>
      <h1>Bem-vindo(a), <%= usuario.nome %>!</h1>
      <p>Gerencie suas anotações abaixo.</p>
      <div class="actions">
        <a href="/criar" class="btn btn-primary">Criar Nova Anotação</a>
        <a href="/<%= usuario.id %>/exportar/txt" class="btn btn-secondary">Exportar para .txt</a>
        <a href="/<%= usuario.id %>/lixeira" class="btn btn-secondary">Lixeira</a>
        <a href="/<%= usuario.id %>/perfil" class="btn btn-secondary">Meu Perfil</a>
        <a href="/logout" class="btn btn-secondary">Sair</a>
      </div>

      <hr style="margin: 30px 0;">

      <div class="group-formulario" style="margin-bottom: 30px;">
        <label for="filtro-anotacoes">Buscar Anotações</label>
        <input type="text" id="filtro-anotacoes" class="campo-formulario" placeholder="Digite para filtrar...">
      </div>

      <h2>Suas Anotações</h2>
      <% if (anotacoes.length > 0) { %>
        <ul class="lista-anotacoes">
          <% anotacoes.forEach(function(anotacao) { %>
            <li class="item-anotacao"> 
              <a href="/<%= usuario.id %>/<%= anotacao.id %>" class="link-anotacao">
                <span class="nome-anotacao"><%= anotacao.nome %></span>
              </a>
              <div class="anotacao-actions">
                <a href="/<%= usuario.id %>/<%= anotacao.id %>/editar" class="acao-anotacao" title="Editar">
                  <i class="fas fa-pencil-alt"></i></a>
                <a href="/<%= usuario.id %>/<%= anotacao.id %>/excluir" class="acao-anotacao" title="Excluir">
                  <i class="fas fa-trash-alt"></i></a>
              </div>
            </li>   
        <% }); %>
        </ul>
      <% } else { %>
        <p>Você ainda não tem nenhuma anotação. Que tal criar a primeira?</p>
      <% } %>
    <% } else { %>
      
      <h1>Erro</h1>
      <p>Você precisa estar logado para ver esta página.</p>
      <a href="/login">Fazer Login</a>
    <% } %>
  </div>
  <script src="/javascripts/filtro.js"></script>
</body>
</html>