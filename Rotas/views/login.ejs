<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
    <div class="painel-formulario">
        <div class="voltar">
            <a href="/">&larr; </a>
        </div>
        <h1>Login</h1>

        <% if (messages && messages.length > 0) { %>
            <div class="alerta-erro">
                <%= messages[0] %>
            </div>
        <% } %>
        <p>Bem-vindo de volta! Acesse sua conta.</p>

        <form action="/login" method="POST">
            <div class="grupo-formulario">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" class="campo-formulario" required>
            </div>
            <div class="grupo-formulario">
                <label for="senha">Senha</label>
                <input type="password" id="senha" name="senha" class="campo-formulario" required>
            </div>
            <button type="submit" class="btn btn-primary btn-largo">Entrar</button>
        </form>
        <div class="rodape-formulario">
            <p>Não tem uma conta? <a href="/cadastro">Cadastre-se</a></p>
        </div>
    </div>
</body>
</html>